query {{ camelCase componentType }}{{ pascalCase name }}($id: String!, $locale: String) {
  {{ graphqlQueryPrefix }}{{ pascalCase name }}(id: $id, locale: $locale) {
    ...CtfComponentFragment
  }
}
