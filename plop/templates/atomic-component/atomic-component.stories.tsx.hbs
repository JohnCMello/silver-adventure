import type { Meta, StoryObj } from '@storybook/react'
import { {{ pascalCase name }} as {{ pascalCase name }}Component } from './{{ kebabCase name }}'

const meta: Meta<typeof {{ pascalCase name }}Component> = {
  component: {{ pascalCase name }}Component,
  title: '{{ titleCase level }}/{{ titleCase name }}',
}
export default meta

export const {{ pascalCase name }}: StoryObj<typeof {{ pascalCase name }}Component> = {}
